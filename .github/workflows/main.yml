name: WakaTime Readme

on:
  workflow_dispatch:
  schedule:
    - cron: '30 18 * * *'

jobs:
  update-readme:
    name: Update README
    runs-on: ubuntu-latest
    steps:
      - uses: anmol098/waka-readme-stats@master
        with:
          WAKATIME_API_KEY: ${{ secrets.waka_b1ad9e06-5cc9-4397-9e94-39226d1e6b1b }}
          GH_TOKEN: ${{ secrets.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 }}
          SHOW_LOC_CHART: False
