name: WakaTime Readme

on:
  workflow_dispatch:
  schedule:
    - cron: '30 18 * * *'

jobs:
  update-readme:
    name: Update README
    runs-on: ubuntu-latest
    steps:
      - uses: anmol098/waka-readme-stats@master
        with:
          WAKATIME_API_KEY: ${{ secrets.waka_2bdea613-a47c-451c-a1f5-191a13138a4d }}
          GH_TOKEN: ${{ secrets.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 }}
          SHOW_LOC_CHART: False
